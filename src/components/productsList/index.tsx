import React from 'react'

import { Container } from './styles'

interface IProductList {
    items: any[];
}

const ProductList: React.FC<IProductList> = ({ items }) => {
    return (
        <Container>

        </Container>
    )
}

export default ProductList